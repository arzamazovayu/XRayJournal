<Window x:Class="XRayJournal.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XRayJournal.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dgPatients" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="0,0,0,0" Grid.Row="1" />
        <Menu>
            <MenuItem Header="Меню">
                <MenuItem x:Name="Exit" Header="Закрыть программу" Click="Exit_Click"/>
            </MenuItem>
        </Menu>
        <Button x:Name="AddWinButton" Content="Добавить запись" HorizontalAlignment="Left" Margin="25,10,0,0" Grid.Row="2" VerticalAlignment="Top" Height="25" Width="100"/>

    </Grid>
</Window>
